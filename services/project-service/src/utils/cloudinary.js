/**
 * ============================================
 * CLOUDINARY UPLOAD SERVICE
 * ============================================
 * Inherits from the shared Cloudinary utility to prevent code duplication.
 */

const { cloudinaryService } = require("../../../../shared");

module.exports = cloudinaryService;
